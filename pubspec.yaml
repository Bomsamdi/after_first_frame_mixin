name: after_first_frame_mixin
description: >-
  Run code after the first layout of a widget has been performed, i.e. after the
  first frame has been displayed - as a mixin or as a widget.
version: 1.0.0
homepage: https://github.com/Bomsamdi/after_first_frame_mixin
repository: https://github.com/Bomsamdi/after_first_frame_mixin
issue_tracker: https://github.com/Bomsamdi/after_first_frame_mixin/issues

topics:
  - widget
  - lifecycle
  - mixin
  - scheduler

environment:
  sdk: ^3.8.0
  flutter: '>=3.32.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
